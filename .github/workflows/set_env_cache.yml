name: Set environment cache
on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:
  
jobs:
  setup:
    runs-on: macos-latest
    env:
      R_LIBS_USER: ${{ github.workspace }}/R/library
    steps:
      - uses: actions/checkout@v4  # Update to v4
      
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'
          
      - uses: r-lib/actions/setup-pandoc@v2
      
      - name: Install system dependencies
        run: |
          brew install gdal udunits
          brew install pkg-config
          brew install qpdf || true
          
      - name: Add cached R packages
        uses: actions/cache@v3
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('**/DESCRIPTION') }}-1  # Added -1 to version the cache
          restore-keys: |
            ${{ runner.os }}-r-
            
      - name: Install R packages
        run: |
          install.packages(c(
            "tidyverse", "stringr", "janitor", "readr", "dplyr",
            "sf", "leaflet", "htmlwidgets", "leaflet.providers",
            "leaflet.extras", "lubridate", "htmltools"
          ), repos = "https://cloud.r-project.org")
        shell: Rscript {0}
